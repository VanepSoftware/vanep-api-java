# Rules

1. **Before running any `explore`, `propose`, or `apply` OpenSpec command (or the corresponding `/opsx:explore`, `/opsx:propose`, `/opsx:apply` slash commands), read the entire `constitution.md` at the project root first.** This applies to both Claude and Cursor. Never skip this step, and never let it be skipped by a partial read — the whole file must be read before any exploration, proposal, or implementation work begins.
